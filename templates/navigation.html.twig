<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <a class="navbar-brand" href="{{ path('home_page') }}">
    <img src="{{ asset('images/logos/logo_light.svg') }}"
      width="30"
      height="30"
      class="d-inline-block align-top"
      alt="" />
    Igris
  </a>
  <button class="navbar-toggler"
    type="button"
    data-toggle="collapse"
    data-target="#navbarColor01"
    aria-controls="navbarColor01"
    aria-expanded="false"
    aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse justify-content-between"
    id="navbarColor01">
    <ul class="navbar-nav mr-auto">
      {% if app.user %}
        <li class="nav-item">
          <a class="nav-link" href="{{ path('manga_index') }}">{{ 'Manga'|trans }}</a>
        </li>

        <li class="nav-item">
          <a class="nav-link" href="{{ path('serie_index') }}">{{ 'Serie'|trans }}</a>
        </li>
      {% endif %}
    </ul>
    <ul class="navbar-nav">
      {% if app.user %}
        {% if is_granted('ROLE_ADMIN') %}
          <li class="nav-item">
            <a href="{{ path('admin_index') }}" class="nav-link">
              <i class="fas fa-cog"></i> {{ 'Administration'|trans }}
            </a>
          </li>
        {% endif %}
        <li class="nav-item">
          <a href="{{ path('security_logout') }}" class="nav-link">
            <i class="fas fa-sign-out-alt"></i> {{ 'Logout'|trans }}
          </a>
        </li>
      {% endif %}
    </ul>
  </div>
</nav>
